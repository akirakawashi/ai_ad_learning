"""Детектор рекламных щитов: псевдоразметка, набор, проверки, обучение.

Порядок работы — по модулям:

1. `prelabel` — прогнать фотографии через рабочую модель, получить разметку и отчёт;
2. `bundle` — собрать из спорных кадров пачки под задачи CVAT;
3. `dataset` — сложить ручную и уверенную машинную разметку в набор из трёх частей;
4. `checks` — проверить набор перед обучением;
5. `preview` — посмотреть разметку глазами;
6. `train` — обучить и сравнить веса.

Настройки всех шагов собраны в `config`.
"""
